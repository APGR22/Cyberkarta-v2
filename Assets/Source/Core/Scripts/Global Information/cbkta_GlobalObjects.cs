using UnityEngine;

public class cbkta_GlobalObjects : MonoBehaviour
{
    public GameObject player;

    [HideInInspector] public GameObject playerTriggeredWithObject = null;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
