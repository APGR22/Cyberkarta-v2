using UnityEngine;

public class SceneFightDialogDataTemplate : DialogueTemplate
{
    //cuma mengikuti DialogueTemplate saja

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
